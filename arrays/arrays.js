// adding something in in an array
const myArray = ["kampala", "mbara" ];
// this is amethod
myArray.push("nairobi");
console.log(myArray);


// using index
const cities=["kampala", "mbara"]
cities[2]="mombasa";
console.log(cities)

// using length
const city=["kampala", "mbara"]
city[city.length -1]="dodoma";
console.log(city)
