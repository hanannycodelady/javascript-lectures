const person = {firstName:"John", lastName:"Doe", age:50, eyeColor:"blue"};
const car ={model_name: 'volvo'}
const car2 = {"brand name":'toyota'}
// the car is the object name and the model_name is the key name 
console.log(car.model_name)
// this you can only use it not to return an error
console.log(car2["brand name"]) 
